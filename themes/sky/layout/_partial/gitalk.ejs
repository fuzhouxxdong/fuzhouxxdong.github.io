<% if(page.comments && theme.comments.gitalk.enable) {%>
<link rel="stylesheet" href="https://unpkg.com/gitalk/dist/gitalk.css">
<script src="https://unpkg.com/gitalk/dist/gitalk.min.js"></script>
<div id="gitalk-container"></div>
<script>
    const gitalk = new Gitalk({
        clientID: '<%=theme.comments.gitalk.id%>',
        clientSecret: '<%=theme.comments.gitalk.secret%>',
        repo: '<%=theme.comments.gitalk.repo%>',
        owner: '<%=theme.comments.gitalk.owner%>',
        admin: ['<%=theme.comments.gitalk.owner%>'],
        language: 'en',
        id: location.pathname,      // Ensure uniqueness and length less than 50
        distractionFreeMode: false  // Facebook-like distraction free mode
    })
    gitalk.render('gitalk-container')
</script>

<%}%>
